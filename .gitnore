# ── Database — NEVER commit these ────────────────────────────────────────────
# If the DB gets committed, `git reset --hard` during deploy will overwrite the
# live database and destroy real bookings and finances.
bookings.db
bookings.db-wal
bookings.db-shm
*.db
*.db-wal
*.db-shm
*.migrated-*
backups/

# Secrets
.flask_secret
.env

# Python
venv/
__pycache__/
*.pyc

# OS
.DS_Store